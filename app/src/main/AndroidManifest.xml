<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.com.usinasantafe.plm">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ParadaActivity"></activity>
        <activity android:name=".AtividadeActivity" />
        <activity android:name=".OSActivity" />
        <activity android:name=".OperadorActivity" />
        <activity android:name=".TurnoActivity" />
        <activity android:name=".EquipActivity" />
        <activity android:name=".ConfiguracaoActivity" />
        <activity android:name=".MenuInicialActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>